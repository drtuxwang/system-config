#!/usr/bin/env bash
#
# appimage-builder virtualenv
#

virtualenv_setup() {
    VENV_PYTHON="python3"
    VENV_PACKAGE="appimage-builder==1.1.0"
    VENV_CLEAN=no
}


source "${0%/*}/venv_mod.bash" -noclean
