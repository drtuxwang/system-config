#!/bin/sh
#
# Wrapper for generic Python 3.x program
#

BIN_DIR=`dirname "$0"`
PYTHON="$BIN_DIR/python3"
if [ ! -x "$PYTHON" ]
then
    PYTHON="python3"
fi
PY_MAIN=`basename "$0"`

exec "$PYTHON" -B -E "$BIN_DIR/pyld.py" $PY_MAIN "$@"
