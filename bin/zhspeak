#!/bin/sh
#
# Wrapper for generic Python program to prefer Python 3
#

BIN_DIR=`dirname "$0"`
PY_MAIN=`basename "$0"`

case `python3 --version 2>&1 | awk '{print $NF}'` in
3.*)
    exec python3 "$BIN_DIR/pyld.py" $PY_MAIN "$@"
    ;;
*)
    exec python $0.py "$@"
    ;;
esac
