#!/usr/bin/env bash
#
# devpi-client virtualenv
#

PYTHON="python3.11"
PACKAGE="devpi-client==5.2.3"
DEPENDS="devpi-common==3.6.0"  # Requires insecure py (1.11.0 last version and seems abandoned)
POSTINST=

source "${0%/*}/venv_mod.bash"
exec "${0##*/}" "$@"
