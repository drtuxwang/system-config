#!/bin/sh
#
# Wrapper for "pep8" command
#

BIN_DIR=`dirname "$0"`
PY_MAIN=`basename "$0"`

if [ -x "$BIN_DIR/python3" ]; then
    exec "$BIN_DIR/python3" $BIN_DIR/py$PY_MAIN.py "$@"
else
    exec $BIN_DIR/py$PY_MAIN.py "$@"
fi
