#!/bin/sh
#
# Wrapper for generic Python 3.x program (without pyloader.py)
#

BIN_DIR=`dirname "$0"`
PY_MAIN=`basename "$0"`

if [ -x "$BIN_DIR/python3" ]; then
    exec "$BIN_DIR/python3" $0.py "$@"
else
    exec $0.py "$@"
fi
