#!/bin/sh
#
# Wrapper for g++generic Python 3.x program
#

BIN_DIR=`dirname "$0"`
PY_MAIN="-pyldname=g++ gxx"

if [ -x "$BIN_DIR/python3" ]; then
    exec "$BIN_DIR/python3" "$BIN_DIR/pyld.py" $PY_MAIN "$@"
else
    exec "$BIN_DIR/pyld.py" $PY_MAIN "$@"
fi
