#!/usr/bin/env bash
#
# awscli virtualenv
#

virtualenv_setup() {
    VENV_PYTHON="python3"
    VENV_PACKAGE="awscli==1.33.44"
    VENV_DEPENDS="PyYAML==6.0.2"  # >=6 still not supported
}


source "${0%/*}/venv_mod.bash"
