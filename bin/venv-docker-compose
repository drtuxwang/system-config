#!/usr/bin/env bash
#
# docker-compose virtualenv
#

virtualenv_setup() {
    VENV_PYTHON="python3.9"                # >= 3.10 still not supported
    VENV_PACKAGE="docker-compose==1.28.6"
    VENV_DEPENDS="PyYAML==5.4.1"           # >=6 still not supported
}


source "${0%/*}/venv_mod.bash"
