#!/bin/bash
#
# Docker container init script
#

mkdir -p "$HOME"
for FILE in /home/home*.tar
do
    tar xf $FILE -C $HOME
    rm -f $FILE
done

if [ "$XAUTH" ]
then
    touch $HOME/.Xauthority
    xauth add $(uname -n)/${XAUTH#*/}
fi

if [ $# = 0 ]
then
    exec bash -l
fi
exec "$@"
