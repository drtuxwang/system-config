#!/bin/bash
#
# Docker container init script
#

# Change docker user ID
if [ "$UID" != 0 ]; then
    sed -i "s/docker:x:1000:/docker:x:$UID:/" /etc/passwd
    chown -R $UID:100 /home/docker
fi

# Run using docker user account
sudo -u docker bash -l
