FROM drtuxwang/debian:8

# Install extra packages
RUN apt-get install --no-install-recommends -y \
		csh \
		file \
		ksh \
		python3 \
		python3-dev \
		python3-pip \
		sudo \
		tcsh \
		tmux \
		wget && \
	python3 -m pip install pep8 pylint && \
	apt-get clean
