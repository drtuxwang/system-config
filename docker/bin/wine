#!/bin/bash

case ${1,,} in
*.bat|cmd|*.cmd|*.com|*.exe|.msi)
    export COMMAND="${0##*/} $@"
    ;;
?*)
    export COMMAND=" $@"
    ;;
*)
    export COMMAND="${0##*/} cmd"
    ;;
esac

export FLAGS="--volume $PWD:$PWD --workdir $PWD"

make -C ${0%/*}/../ubuntu-18.04-i386/wine run
exit $?
