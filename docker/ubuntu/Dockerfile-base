FROM drtuxwang/ubuntu:latest

# Install extra packages
RUN apt-get install --no-install-recommends -y \
		csh \
		ksh \
		python3-dev \
		python3-pip \
		python3-setuptools \
		sudo \
		tcsh \
		tmux \
		wget && \
	python3 -m pip install pycodestyle pep8==1.7.0 pylint && \
	apt-get clean
