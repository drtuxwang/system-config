ansible-core==None           # Requires cryptography which is broken
ansible==None                # Requires cryptography which is broken
cryptography==None           # Broken on mac
Glances==None                # Requires psutil which is broken
paramiko==None               # Requires cryptography which is broken
psutil==None                 # Broken on mac
pyOpenSSL==None              # Requires cryptography which is broken
