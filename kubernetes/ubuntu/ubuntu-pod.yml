apiVersion: v1
kind: Pod
metadata:
  labels:
    app: ubuntu-lxc
  name: ubuntu-pod
  namespace: default
spec:
  containers:
  - command:
    - bash
    env:
    - name: NAME
      value: VALUE
    image: ubuntu:16.04
    imagePullPolicy: IfNotPresent
    name: ubuntu-lxc
    ports:
    - containerPort: 80
      protocol: TCP
    resources:
      limits:
        cpu: 1000m
        memory: 1024M
      requests:
        cpu: 100m
        memory: 64M
    volumeMounts:
    - mountPath: /shared
      name: shared
  restartPolicy: Never
  volumes:
  - hostPath:
      path: /shared
    name: shared
