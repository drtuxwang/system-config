apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: ubuntu
    matchLabels: ubuntu
  name: ubuntu
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ubuntu
  template:
    metadata:
      labels:
        app: ubuntu
    spec:
      containers:
      - command:
        - sleep
        - '86400'
        env:
        - name: NAME
          value: VALUE
        image: drtuxwang/ubuntu:18.04
        imagePullPolicy: IfNotPresent
        name: ubuntu
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          limits:
            cpu: 1000m
            memory: 1024M
          requests:
            cpu: 100m
            memory: 64M
        volumeMounts:
        - mountPath: /shared
          name: shared
      nodeSelector:
        kubernetes.io/hostname: hotdog.local
      terminationGracePeriodSeconds: 10
      volumes:
      - hostPath:
          path: /shared
        name: shared
